rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write
    }
  }
}

service firebase.storage {
 match /b/{bucket}/0 {
   match /{allPaths=**} {
    allow read, write;
   }
 }
}
